# Profile switch → FABLE chair

The Fable 5 limit is available again; Fable holds the chair.
Effective immediately: the fable tier resumes as the escalation
ceiling above opus, and it spends the chair's own limit, so opus
still takes the hard slices directly. Every other rule from the
already-injected core profile stays in force — above all, delegate
by default.
